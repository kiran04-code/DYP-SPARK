<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <%- include("partial/head") %>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
     .alert-danger{
        margin-top: 20px;

     }
      .text-center{
     font-family: futura;
     }
</style>
<body class="bg-gradient-to-br from-blue-700 to-blue-400 min-h-screen flex items-center justify-center px-4">

  <div class="bg-white max-w-sm w-full p-6 rounded-2xl shadow-lg">
    <h2 class="text-2xl font-bold text-center text-blue-700 mb-6">Create Account</h2>
    
    <form action="/signup" method="post" class="space-y-4 text-sm">
      <div>
        <label for="name" class="block mb-1 text-gray-700 font-medium">Name</label>
        <input type="text" name="UserName" id="name" required
        class="w-full px-3 py-2 border border-blue-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>

      <div>
        <label for="email" class="block mb-1 text-gray-700 font-medium">Email</label>
        <input type="email" name="email" id="email" required
        class="w-full px-3 py-2 border border-blue-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>

      <div>
        <label for="number" class="block mb-1 text-gray-700 font-medium">Phone</label>
        <input type="tel" name="Number" id="number" required
        class="w-full px-3 py-2 border border-blue-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>
      <div>
        <label for="number" class="block mb-1 text-gray-700 font-medium">Age</label>
        <input type="tel" name="Age" id="age" required
        class="w-full px-3 py-2 border border-blue-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>

      <div>
        <label for="password" class="block mb-1 text-gray-700 font-medium">Password</label>
        <input type="password" name="password" id="password" required
        class="w-full px-3 py-2 border border-blue-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>
      <button type="submit"
  class="w-full bg-blue-600 text-white py-2.5 rounded-lg font-semibold hover:bg-blue-500 transition">
  Sign Up
</button>

    </form>
   <% if (locals.error) { %>
    <div class="alert alert-danger" role="alert">
       <%= locals.error %>
      </div>
   <% } %>
      
  </div>

</body>
</html>
