<!DOCTYPE html>
<html lang="en">
<head>

  <title>User Profile</title>
  <%- include('partial/head') %>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet"/>

  <style>
    .profile-card {
      background: white;
      border-radius: 16px;
      padding: 40px 30px;
      max-width: 420px;
      width: 100%;
      box-shadow: 0px 8px 25px rgba(0, 0, 0, 0.05);
      text-align: center;
      position: absolute; /* or fixed */
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
    }

    .profile-img {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      object-fit: cover;
      background-color: #e9ecef;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 36px;
      color: #6c757d;
      margin: 0 auto 20px auto;
    }

    .user-name {
      font-size: 24px;
      font-weight: 600;
      margin-bottom: 5px;
    }

    .user-info {
      color: #555;
      font-size: 16px;
      margin-bottom: 8px;
    }

    .logout-btn {
      background-color: rgba(23, 17, 220, 0.93);
      color: white;
      padding: 10px 18px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 16px;
      width: 100%;
      transition: 0.3s ease;
      margin-top: 25px;
    }

    .logout-btn:hover {
      background-color: rgb(24, 51, 190);
    }
  </style>
</head>
<body>
   
  <div class="profile-card">
    <!-- Avatar Placeholder -->
    <div class="profile-img">
      <i class="fa-regular fa-user"></i>
    </div>

    <div class="user-name">Welcome, <%= user.UserName %></div>

    <div class="user-info"><i class="fa-solid fa-envelope me-2"></i><%= user.email %></div>
    <div class="user-info"><i class="fa-solid fa-phone me-2"></i><%= user.Number %></div>

    <a href="/logout"><button class="logout-btn"><i class="fa-solid fa-right-from-bracket me-2"></i>Logout</button></a>
  </div>

</body>
</html>
